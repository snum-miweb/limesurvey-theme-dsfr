{# Modale Gestion des cookies #}
<dialog id="cookies-modal"
        class="fr-modal"
        aria-labelledby="cookies-modal-title"
        aria-modal="true">
    <div class="fr-container fr-container--fluid fr-container-md">
        <div class="fr-grid-row fr-grid-row--center">
            <div class="fr-col-12 fr-col-md-10">
                <div class="fr-modal__body">
                    <div class="fr-modal__header">
                        <button type="button"
                                class="fr-btn--close fr-btn"
                                aria-controls="cookies-modal">
                            {{ gT("Close") }}
                        </button>
                    </div>
                    <div class="fr-modal__content">
                        <h1 id="cookies-modal-title" class="fr-modal__title">
                            {{ gT("Gestion des cookies") }}
                        </h1>

                        {% if aSurveyInfo.options.cookies_content %}
                            {{ aSurveyInfo.options.cookies_content|raw }}
                        {% else %}
                            <h2>Cookies utilisés</h2>
                            <p>Ce site utilise les cookies suivants :</p>

                            <h3>Cookies strictement nécessaires</h3>
                            <ul>
                                <li><strong>Session LimeSurvey</strong> : permet de conserver votre progression dans le questionnaire</li>
                                <li><strong>Langue</strong> : mémorise votre choix de langue</li>
                            </ul>

                            <h3>Autres cookies analytiques</h3>
                            <p>Aucun autre cookie analytique n'est utilisé sur ce site.</p>


                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</dialog>
